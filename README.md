# web-research-mcp (v2, work in progress)

Web search and verbatim page reading for LLM agents: MCP server, CLI, and library.

This branch is a from-scratch rebuild. The previous SearXNG-based implementation is preserved at the
`legacy-v0.5.0` tag. Design documents live in [docs/design](docs/design/README.md).
