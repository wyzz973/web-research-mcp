# 工具编排

webfetch 读取安全加载器并保存不可变格式快照，游标读取不重新出网。websearch 先采集有界候选池、域过滤与去重，再按页获取可选原文证据。候选池与页响应分别不可变存储；相同续页并发共享执行，首调用取消不取消其他调用。

总超时且已有候选保留 partial；用户取消等待已启动的证据任务全部清理后返回。错误由 common.ts 映射，最终响应通过 Schema 校验。
